import React from 'react';

const StartSectionWrapper = ({ children }) => {
    return (
        <div className="home">
            {children}
        </div>
    );
};
export default StartSectionWrapper